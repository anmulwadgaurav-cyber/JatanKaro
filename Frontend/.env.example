# Frontend environment variables (Vite)
# Copy to `.env` at project root of Frontend

# Base URL for backend API (no trailing slash), e.g. https://jatankaro.onrender.com
VITE_API_BASE=https://jatankaro.onrender.com
